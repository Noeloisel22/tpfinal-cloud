trigger:
  - develop

variables:
  - group: 'tpfinal-vars'

jobs:
  - job: BuildAndPackage
    pool:
      vmImage: 'ubuntu-latest'
    steps:
      - template: .azure/pipelines/templates/_build-front.yml